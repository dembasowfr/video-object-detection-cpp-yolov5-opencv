#ifndef UTILS_H
#define UTILS_H

#include <vector>
#include <string>


//Loads the class list from the given file path and stores it in a vector of strings
std::vector<std::string> loadClassList(const std::string& filePath);

#endif // UTILS_H
